import React from 'react';

import ListDropdown from '../../components/View/List';

const listDropdownContainer = (Element) => {

    return <Element {...this.props} />
};

export default listDropdownContainer(ListDropdown);