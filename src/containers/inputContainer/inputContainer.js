import React from 'react';

import InputDropdown from '../../components/View/Input';

const inputDropdownContainer = (Element) => {

    return <Element {...this.props}/>
};

export default inputDropdownContainer(InputDropdown);